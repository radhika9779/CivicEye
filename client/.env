# Only needed if your backend isn't on localhost:5000 (e.g. after deploying).
# Locally, socketStore.js auto-derives this from the page origin, so you can
# leave this unset for local dev.
VITE_SOCKET_URL=http://localhost:5000
